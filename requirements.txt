# Core RAG stack
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0
PyPDF2>=3.0.1
python-docx>=1.1.0
openpyxl>=3.1.2

# LLM + agent
# NOTE: pinned to the 0.3.x line because langchain 1.0 removed
# `initialize_agent` and `AgentType`, which this project still uses.
openai>=1.30.0,<2.0
langchain>=0.3.20,<0.4
langchain-community>=0.3.0,<0.4
langchain-openai>=0.2.0,<0.4

# Web search tool
requests>=2.32.0
beautifulsoup4>=4.12.0

# Desktop UI
pywebview>=5.0

# Config
python-dotenv>=1.0.1
