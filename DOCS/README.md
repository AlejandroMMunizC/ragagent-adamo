# DOCS folder

The agent indexes every supported file dropped into this folder.

Supported extensions: `.pdf`, `.docx`, `.xlsx`, `.xls`.

The repository ships with three small fictional samples
(`sample_company.pdf`, `sample_products.xlsx`, `sample_faq.docx`) so the
project runs out of the box. Delete them and add your own documents to use
the agent over your real data.
